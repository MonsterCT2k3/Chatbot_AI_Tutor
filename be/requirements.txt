fastapi
uvicorn
pydantic[email]
pydantic-settings
python-dotenv
python-multipart

# Database (Supabase Postgres + pgvector)
sqlalchemy[asyncio]
asyncpg
alembic
pgvector

# File storage (Cloudflare R2, S3-compatible)
boto3

# Auth
bcrypt
python-jose[cryptography]
slowapi

# LLM (gpt-4o-mini) + embeddings
openai

# Local reranker (cross-encoder, multilingual incl. Vietnamese) for RAG retrieval
sentence-transformers

# HTTP client for Voyage AI Rerank API (no official Python SDK dependency
# wanted just for 1 endpoint — plain REST call is enough). Already an
# installed transitive dep of openai/httpx-based SDKs; listed explicitly here
# because rag_service.py now imports it directly.
httpx

# OCR text extraction mode (alternative to pypdf, handles embedded images/diagrams)
mistralai

# Document parsing/chunking (PDF ingestion — PPTX is converted to PDF via
# LibreOffice headless first, then goes through the same PDF pipeline)
pypdf
tiktoken

# Renders page 1 to a PNG thumbnail once at ingest time (dashboard card
# preview) — pure binary wheel, no system poppler/mupdf install needed unlike
# pdf2image/pymupdf.
pypdfium2

# Only used to generate a throwaway .pptx test fixture in step 3.2's test —
# not used for real parsing (PPTX is converted to PDF, not read directly)
python-pptx

# Web search fallback (kept from prior RAG service)
ddgs
