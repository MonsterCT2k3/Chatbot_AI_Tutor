[← Tất cả các bước Phase 6](README.md)

## 6.9 — Test end-to-end

Không thêm code production. Chỉ chạy `send_message` + LLM thật trên `b1-full.pdf`.

**Phạm vi:** hai việc chưa đo trên đường production — (1) 2 session không lẫn lịch sử, (2) follow-up không nhắc lại chủ đề vẫn retrieve đúng nhờ 6.7.  
**Không làm:** đốt 50 câu cho 429; lặp 404/cursor/`updated_at` (đã có 6.2–6.6).

---

### Kết quả (2026-08-25)

| Case | Kết quả |
|---|---|
| S1: *“RLHF gồm mấy bước chính?”* | “ba bước” · citation **trang 39** |
| S1 follow-up: *“bước thứ hai là gì?”* (không viết RLHF) | Rewrite: *“Bước thứ hai trong quy trình RLHF là gì?”* · trả lời người chấm xếp hạng · **trang 39** |
| S2: *“Transformer ra đời vào năm nào?”* | 2017 · **trang 22** |
| Lẫn lịch sử | S1 không có Transformer; S2 không có RLHF |
| `created_at` | user < assistant trên cả hai session |
| `GET` list | S2 (vừa chat) đứng đầu |
| User khác | `SessionNotFoundError` → router 404 |
| `answer_id` | có trên tin assistant S1 |

`ai_call_log` follow-up có `call_type=contextualize`, cùng group với lượt hỏi.

Phase 6 backend (session + multi-turn retrieve) khép. Còn nối frontend và Phase 7 SSE.
